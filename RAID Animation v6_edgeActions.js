/***********************
* Adobe Edge Animate Composition Actions
*
* Edit this file with caution, being careful to preserve 
* function signatures and comments starting with 'Edge' to maintain the 
* ability to interact with these actions from within Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // aliases for commonly used Edge classes

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
      

      

      

      

      

      

      

            Symbol.bindElementAction(compId, symbolName, "${_RAID_10}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("RAID_10").play();
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_RAID_10}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("RAID_10").play(501);
         
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_RAID_6}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("RAID_6").play();
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_RAID_6}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("RAID_6").play(501);
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_RAID_5}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("RAID_5").play();
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_RAID_5}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("RAID_5").play(501);
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_RAID_1}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("RAID_1").play();
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_RAID_1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("RAID_1").play(501);
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_RAID_0}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("RAID_0").play(501);
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_RAID_0}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("RAID_0").play();
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_RAID_0}", "click", function(sym, e) {
         // insert code for mouse click here
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(100);
         

      });
         //Edge binding end



            Symbol.bindElementAction(compId, symbolName, "${_Back}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Back").play();
         

      });
         //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Back}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Back").play(1001);
         

      });
         //Edge binding end


      

      

      Symbol.bindElementAction(compId, symbolName, "${_Insert_info_R0}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Insert_info_R0").play();
         

      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${_Insert_info_R0}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Insert_info_R0").play(501);
         
         
         

      });
      //Edge binding end

      

      Symbol.bindElementAction(compId, symbolName, "${_Insert_info_R0}", "click", function(sym, e) {
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(1100);
         
         

      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive1_R0}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Delete_drive1_R0").play();
         

      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive1_R0}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Delete_drive1_R0").play(501);
         

      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive2_R0}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Delete_drive2_R0").play();
         

      });
      //Edge binding end

      

            Symbol.bindElementAction(compId, symbolName, "${_Back}", "click", function(sym, e) {
         // insert code for mouse click here
         // Play the timeline at a label or specific time. For example:
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(0);
         
         // sym.play(500); or sym.play("myLabel");
         //sym.play(0);
         
         
         
         
         
         
         
         

      });
         //Edge binding end

Symbol.bindElementAction(compId, symbolName, "${_Delete_drive2_R0}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Delete_drive2_R0").play(501);
         

      });
      //Edge binding end
      
      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive2_R0}", "click", function(sym, e) {
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(6401);
         
         

      });
      //Edge binding end

      

      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive1_R0}", "click", function(sym, e) {
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(5900);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 69, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(100);

      });
      //Edge binding end

      

      

      

      

      

      

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 7691, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(7690);

      });
      //Edge binding end

      

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 8700, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(8700);

      });
      //Edge binding end

      

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 5800, function(sym, e) {
         // insert code here
         sym.stop(5800);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1099, function(sym, e) {
         // insert code here
         sym.stop(1099);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 6400, function(sym, e) {
         // insert code here
         sym.stop(6400)

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 6910, function(sym, e) {
         // insert code here
         sym.stop(6910)

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive1}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Delete_drive1").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Delete_drive1").play(501);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive1}", "click", function(sym, e) {
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(15900);

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive2}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Delete_drive2").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive2}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Delete_drive2").play(501);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Delete_drive2}", "click", function(sym, e) {
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(16501);
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Insert_info_R1}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Insert_info_R1").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Insert_info_R1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Insert_info_R1").play(501);
         
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Insert_info_R1}", "click", function(sym, e) {
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(11100);
         
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 0, function(sym, e) {
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 10100, function(sym, e) {
         // insert code here
         
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(11100);
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 15801, function(sym, e) {
         // insert code here
         sym.stop(15800);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 16500, function(sym, e) {
         // insert code here
         sym.stop(16500)

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 17010, function(sym, e) {
         // insert code here
         sym.stop(17000)

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 17691, function(sym, e) {
         // insert code here
         sym.stop(17690);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 18709, function(sym, e) {
         // insert code here
         sym.stop(18700);

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_RAID_1}", "click", function(sym, e) {
         // insert code for mouse click here
         sym.play(11000);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 11079, function(sym, e) {
         // insert code here
         
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(11099);

      });
      //Edge binding end

      

      

      

      

      

      

      

      

      

      

      Symbol.bindElementAction(compId, symbolName, "${_Insert_info_R10}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Insert_info_R10").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Insert_info_R10}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Insert_info_R10").play(501);
         
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Insert_info_R10}", "click", function(sym, e) {
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(61100);
         
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 19783, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(19800);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 61089, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(61099);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 65933, function(sym, e) {
         // insert code here
         sym.stop(65900);

      });
      //Edge binding end

      

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 67118, function(sym, e) {
         // insert code here
         
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(67133);
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 67733, function(sym, e) {
         // insert code here
         sym.stop(67730);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 68466, function(sym, e) {
         // insert code here
         sym.stop(68500);

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_RAID_10}", "click", function(sym, e) {
         // insert code for mouse click here
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play(60999);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive1_R10}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object;
         sym.getSymbol("Drive1_R10").play();
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive1_R10}", "mouseout", function(sym, e) {
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Drive1_R10").play(501);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive2_R10}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Drive2_R10").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive2_R10}", "mouseout", function(sym, e) {
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Drive2_R10").play(501);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive3_R10}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Drive3_R10").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive3_R10}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Drive3_R10").play(501);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive4_R10}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Drive4_R10").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive4_R10}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Drive4_R10").play(501);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Clear_R102}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Clear_R102").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Clear_R102}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("Clear_R102").play(501);
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 69500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(69500);

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive2_R10}", "click", function(sym, e) {
         
         sym.getSymbol("disappear_2").play(0);

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive3_R10}", "click", function(sym, e) {
         // insert code for mouse click here
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         
         sym.getSymbol("disappear_3").play(0);
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive4_R10}", "click", function(sym, e) {
         // insert code for mouse click here
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         
         sym.getSymbol("disappear_4").play(0);
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Clear_R102}", "click", function(sym, e) {
         // insert code for mouse click here
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("disappear_1").play(501);
         sym.getSymbol("disappear_2").play(501);
         sym.getSymbol("disappear_3").play(501);
         sym.getSymbol("disappear_4").play(501);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${_Drive1_R10}", "click", function(sym, e) {
         // insert code for mouse click here
         
         sym.getSymbol("disappear_1").play(0);
         
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 66567, function(sym, e) {
         // insert code here
         sym.stop(66567);

      });
      //Edge binding end

   })("stage");
   //Edge symbol end:'stage'

   //=========================================================
   
   //Edge symbol: 'RAID_10'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(1000);

      });
      //Edge binding end

   })("RAID_10");
   //Edge symbol end:'RAID_10'

   //=========================================================
   
   //Edge symbol: 'RAID_6'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("RAID_6");
   //Edge symbol end:'RAID_6'

   //=========================================================
   
   //Edge symbol: 'RAID_5'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("RAID_5");
   //Edge symbol end:'RAID_5'

   //=========================================================
   
   //Edge symbol: 'RAID_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("RAID_1");
   //Edge symbol end:'RAID_1'

   //=========================================================
   
   //Edge symbol: 'RAID_0'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("RAID_0");
   //Edge symbol end:'RAID_0'

   //=========================================================
   
   //Edge symbol: 'Back'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("Back");
   //Edge symbol end:'Back'

   //=========================================================
   
   //Edge symbol: 'Insert_info'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1000, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(1001);

      });
      //Edge binding end

   })("Insert_info");
   //Edge symbol end:'Insert_info'

   //=========================================================
   
   //Edge symbol: 'Delete_drive1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("Delete_drive1");
   //Edge symbol end:'Delete_drive1'

   //=========================================================
   
   //Edge symbol: 'Delete_drive2'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("Delete_drive2");
   //Edge symbol end:'Delete_drive2'

   //=========================================================
   
   //Edge symbol: 'balls'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 4700, function(sym, e) {
         // insert code here
      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 3000, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(1000);

      });
      //Edge binding end

   })("balls");
   //Edge symbol end:'balls'

   //=========================================================
   
   //Edge symbol: 'Disappear'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1000, function(sym, e) {
         // insert code here
      });
      //Edge binding end

   })("Disappear");
   //Edge symbol end:'Disappear'

   //=========================================================
   
   //Edge symbol: 'Disk'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 0, function(sym, e) {
         // insert code here
      });
      //Edge binding end

   })("Disk");
   //Edge symbol end:'Disk'

   //=========================================================
   
   //Edge symbol: 'Drive1_R10'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 490, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("Drive1_R10");
   //Edge symbol end:'Drive1_R10'

   //=========================================================
   
   //Edge symbol: 'Drive2_R10'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("Drive2_R10");
   //Edge symbol end:'Drive2_R10'

   //=========================================================
   
   //Edge symbol: 'Drive3_R10'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("Drive3_R10");
   //Edge symbol end:'Drive3_R10'

   //=========================================================
   
   //Edge symbol: 'Drive4_R10'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("Drive4_R10");
   //Edge symbol end:'Drive4_R10'

   //=========================================================
   
   //Edge symbol: 'Clear_R10'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("Clear_R10");
   //Edge symbol end:'Clear_R10'

   //=========================================================
   
   //Edge symbol: 'disappear_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("disappear_1");
   //Edge symbol end:'disappear_1'

   //=========================================================
   
   //Edge symbol: 'disappear_2'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("disappear_2");
   //Edge symbol end:'disappear_2'

   //=========================================================
   
   //Edge symbol: 'disappear_3'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(501);

      });
      //Edge binding end

   })("disappear_3");
   //Edge symbol end:'disappear_3'

   //=========================================================
   
   //Edge symbol: 'disappear_4'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 500, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop(500);

      });
      //Edge binding end

   })("disappear_4");
   //Edge symbol end:'disappear_4'

})(jQuery, AdobeEdge, "EDGE-55839015");